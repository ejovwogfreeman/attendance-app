import React from 'react'

const Home = ({data}) => {
  return (
    <div style={{marginTop: '80px', marginLeft: '90px'}}>
        Rendering home page...
        {/* {data.map((d)=>{
            return (
                <div>
                    <h1>{d.name}</h1>
                </div>
            )
        })} */}
    </div>
  )
}

export default Home